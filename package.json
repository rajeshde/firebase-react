{
  "name": "firebase-admin",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start-backend": "cd backend && npm run start",
    "start-frontend": "cd frontend && npm run build && serve -s build"
  }
}
